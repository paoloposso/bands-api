steps:
# - name: 'gcr.io/cloud-builders/go'
#   args: ['install', './...']
#   env: ['PROJECT_ROOT=github.com/paoloposso/bands-auth-api']
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: 'bash'
  args: ['-c', 'gcloud config set app/cloud_build_timeout 1600 && gcloud app deploy']
# timeout: '1600s'